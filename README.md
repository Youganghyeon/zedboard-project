(Tetromino)
I O T S

1 STEP fall the block
2 STEP move the left and right
       button에 따라서 left and right
3 STEP various shape
4 STEP change the angle
5 STEP add block
6 step 블럭 쌓기
7 step remove the line (if IsFull_Line ==1 ) -> 모든 채워진 라인을 다 검사해야한다. 즉 제일 아래라에서부터 check
8 step add point
9 step speed (parameter BOX_X ,BOTTOM, BOX_SIZE : parameter -> reg로 변경하면 가능할지도)


button : left, right로 이동, angle 변환을 left나 right로 90도씩 변경해야하므로 총 4개의 button이 필요)

남는 button에 속도를 넣기
